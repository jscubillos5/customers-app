import { AppFooterComponent } from './app.footer.component';

describe('App.Footer.Component', () => {
  it('should create an instance', () => {
    expect(new AppFooterComponent()).toBeTruthy();
  });
});